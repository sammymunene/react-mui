import { Box } from '@mui/material'

const MuiBox = () => {
  return (
    <Box sx={{ width: 200, height: 100, backgroundColor: 'lightblue', padding: 2 }}>
      This is a Box component
    </Box>
  )
}

export default MuiBox
